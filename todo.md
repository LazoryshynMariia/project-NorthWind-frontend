Dev 1 Team Lead (МАРІЯ): розгортання проєкту (тільки інфраструктура)

Ініціалізація Next.js 15 (App Router), структура репозиторію, ESLint/Prettier
Налаштування БД (ORM, міграції, конекшн), CI/CD Global error.tsx, not-found.tsx
Базова обгортка глобального лоадера та toast-системи (провайдер для всіх) Захист
приватних маршрутів (логіка на рівні компонентів) Shared DTO/утиліти для інших
модулів Налаштування сервісу зберігання файлів (S3/Cloudinary/UploadThing)
README проєкту повністю

Dev 2 (Maria-Nina) Auth-ядро Frontend:

LoginPage, LoginForm (Formik + Yup) Backend:

Auth-модуль: login, JWT (генерація + перевірка), refresh-токен Схема користувача
в БД, хешування паролів Middleware перевірки токена

Dev 3 (Анастасія) — Реєстрація Frontend:

RegisterPage, RegistrationForm (Formik + Yup) AuthHeader, MainAuthNav,
AuthFooter Підключення шрифтів + favicon Backend:

Ендпоінт register (POST /auth/register) — валідація унікальності email, коди
помилок узгоджені з Dev2, використовує схему користувача та хешування паролів
від Dev2

Dev 4 (Marina)— Layout / Header / переходи Frontend:

Layout, Header (адаптив, бургер-меню, 2 стани) AuthBar, UserBar Анімація
бургер-меню, базові transition-патерни проєкту (framer motion конфіг) Backend:

Ендпоінт /me (використовує middleware від Dev2) Ендпоінт logout (інвалідація
токена/refresh-токена)

Dev 5 (Олена ) - Footer, атоми, темна тема Frontend:

Footer, SocialList, Copyright, FooterNav PageTitle, ConfirmModal Темна тема:
CSS-змінні, перемикач, збереження вибору Backend:

Ендпоінт збереження вибору теми (PATCH /users/theme)

Dev 6 (Ivan Vodzianovskyi)  StoryCard + збережені історії Frontend:

StoryCard (лоадер, стилі кнопки, toast помилок) ErrorWhileSavingModal SaveStory
Backend:

CRUD "збережені історії" (додати/видалити/перевірити), схема saved_stories Dev 7
(Людмила Скопенко) Списки, пагінація + PersonalPage Frontend:

TravellersStories (список + пагінація + scrollIntoView) Pagination (лоадер,
disable) MessageNoStories PersonalPage (/profile/edit) Backend:

Ендпоінт GET /stories з пагінацією та фільтром по автору Ендпоінт оновлення
особистих даних (PATCH /users/me/personal)

Dev 8 (Illia Lykhovod) HomePage + Swiper Frontend:

HomePage, Hero, About, Join Базове підключення Swiper для проєкту (конфіг,
стилі) PopularStories (слайдер 3/2/1, disable на краях) Backend:

Ендпоінт "популярні статті" (сортування за к-стю збережень, ліміт 10)

Dev 9 (Yelyzaveta) Мандрівники Frontend:

TravellerCard, TravellerInfo OurTravellers (слайдер 4/4/3, на базі конфігу Dev8)
TravellersPage, TravellersList Backend:

Ендпоінт GET /travellers (список + пагінація) Ендпоінт "топ мандрівників" (12
елементів)

Dev 10 (Cофія Максимчук) Статті: список, фільтр, деталі Frontend:

StoriesPage, CategoriesFilter (кнопки/dropdown мобілка) StoryPage, StoryDetails
RecomendedStories (3/2/1) Backend:

Ендпоінт категорій (GET /categories) Ендпоінт деталей статті (GET /stories/:id,
обробка "не знайдено") Ендпоінт рекомендованих статей   Dev 11( Іван Дзямко)
Профіль (публічний + власний) Frontend:

TravellerPage (публічний профіль, "користувача немає") ProfilePage, ProfileTabs
(паралельні маршрути "Збережені"/"Мої") Backend:

Ендпоінт деталей мандрівника (GET /travellers/:id)

Dev 12 (Kateryna Vyshnevska)Створення історії Frontend:

AddStory, AddStoryForm (upload фото, Formik + Yup, disabled-кнопка) Backend:

Ендпоінт створення статті (POST /stories, завантаження зображення через сервіс
сховища від Dev1 )

я дев 6 делаем только его

вот Макет Природні мандри (багатосторінковий сайт) БАЗОВА ВЕРСІЯ (MVP - minimum
viable product) Базові задачі "Адаптивність - підтримка мобільних, планшетних і
десктопних розмірів екранів (mobile-first підхід).

- mobile: верстка гумова від 320px, стає адаптивною з 375px;
- tablet: з 768px;
- desktop: з 1440px При написанні стилів використаний Mobile First підхід і
  медіа-функція min-width. Стилізація - реалізована через CSS Modules.
  Підключений нормалізатор стилів modern-normalize Всім інтерактивним елементам
  (кнопки, посилання тощо) слід додати hover-ефект згідно макету (відповідно до
  UI Kit). Для ефекту hover клікабельним елементам потрібно додати зміну
  курсору. Маршрутизація - App Router Next.js 15. Реалізація публічних та
  приватних маршрутів (захист сторінок на рівні компонентів). Error/Not-found
  сторінки - в корені app повинні знаходитись глобальні error.tsx і
  not-found.tsx. Компоненти: Server Components - всі за замовчуванням , Client
  Components - лише, коли потрібні (форми, інтеракції, React Query, Zustand).
  Для сторінок підключити generateMetadata, OG теги для сторінок інструмента.
  Реалізувати динамічний імпорт зображень - next/image. Стейт-менеджмент - за
  необхідності використовується Zustand. Отримання даних - за необхідності
  використовується React Query (TanStack Query) для кешування, синхронізації та
  оновлення серверного стану. Індикатори завантаження - відображення лоадера під
  час усіх активних запитів (на рівні сторінки чи окремих компонентів). Обробка
  помилок - повідомлення про помилки запитів та валідації форм (використання
  toast-повідомлень, статус-повідомлень на формі). Форми - створення форм через
  Formik із валідацією на основі Yup." Вихідний код відформатований за допомогою
  Prettier Здійснити підключення шрифтів Додати відображення фавікон сторінки
  (знаходиться в UI KIT) Анімації не є обовʼязковими, але їх використання може
  зробити взаємодію з вебсайтом більш приємною та інтуїтивно зрозумілою для
  користувача (наприклад можна використати бібліотеку framer motion, або gsap)
  Документація проєкту "У репозиторії проєкту має бути файл README, де коротко і
  структуровано описано: назва проєкту; про проєкт і яку задачу він вирішує; які
  технології використані; як запустити/розгорнути проєкт; супутня інформація, що
  стосується проєкту.

ДОДАТКОВО для коректного оформлення README можна скористатися
онлайн-редакторами: https://readme.so/editor https://dillinger.io/" Структура
проєкту (сторінки) "- Реєстрація / Вхід (route: /auth/register)

- Логін / Вхід (route: /auth/login)
- Головна (route: /)
- Історії (route: /stories)
- Історія (route: /stories/[storyId])
- Мандрівники (route: /travellers)
- Профіль Мандрівника Публічний (route: /traveller/[travellerId])
- Збережені / Мої історії (route: /profile)
- Створення історії (route: /stories/new)" Загальні та перевикористовувані
  компоненти "Заголовок сторінки.

Компонент містить в собі:

- текст заголовку сторінки" PageTitle "Компонент відображає картку з коротким
  описом користувача(зареєстрованого автора).

Компонент містить в собі: Аватар, Ім'я, Кількість статей написаних даним
автором, Посилання на сторінку автора - стилізоване під кнопку з текстом
""Переглянути профіль"",

Взаємодія з користувачем: В результаті кліку по кнопці ""Переглянути профіль""
повинна відбутись переадресація на сторінку з детальним інформацією про даного
користувача - маршрут /trevallers/[trevallerId]" TrevallerCard "Компонент
відображає картку з коротким описом статті.

Компонент містить в собі: Зображення, Ім'я автора, Рейтинг - кількість додавань
статті до збережених статей іншими користувачами, Заголовок - назва статті,
Кнопка ""Переглянути статтю"", Кнопка з іконокою для додавання статті до
збережних. Для авторизованого користувача, якщо стаття вже знаходиться в
збережених статтях користувача, кнопка з іконкою працює на видалення статті зі
збережених і отримує стилі відповідно до макету.

Взаємодія з користувачем: В результаті кліку по кнопці ""Переглянути статтю""
повинна відбутись переадресація на сторінку з детальним описом статті - маршрут
/stories/[storyId] В результаті кліку по кнопці з іконокою для додавання статті
до збережних у випадку: НЕавторизаний користувач:

- відкривається модальне вікно ErrorWhileSavingModal Авторизований користувач:
- відбувається запит на додавання/видалення даної статті до/зі збережених статей
  користувача,
- під час запиту відображається індикатор завантаження(лоадер),
- у разі помилки в результаті запиту виводиться пуш-повідомлення з суттю
  помилки,
- у разі успішного запиту кнопка змінює стилі відповідно до макету." StoryCard
  "Компонент відображає список з історіями мандрівників та пагінацію. Дані для
  списку історій повретаються у відповідному запиті до БД.

Компонент містить в собі: Список історій - кожну картку з описом історії
мандрівника реалізовано за допомогою загального компоненту StoryCard.
Pagination - загальний компонент, що виконує запити до БД за відповідними
порціями даних. У компонента повинна бути можливість рендерити різні за
кількістю списки історій.

Взаємодія з користувачем: В результаті помилки на запит користувачу потрібно
вивести пуш-повідомлення з суттю помилки. В результаті успішного запиту, до
списку карток з історіями повинен додатись список карток з історіями дані яких
були отриманими в результаті запиту. Після завантаження останньої порції даних
кнопка повинна зникнути зі сторінки.

ДОДАТКОВО(не обов'зково): список з картками, створеними на основі отриманих
даних в поточному запиті, повинен автоматично проскролитись до верхньої частини
екрану scrollIntoView" TravellersStories "Комопонент відображає кнопку з текстом
""Показати ще"", що забезпечує пагінацію.

Взаємодія з користувачем: В результаті кліку по кнопці повинен відбуватись запит
по наступну порцію даних. Під час запиту повинен відображатись індикатор
завантаження(лоадер). Під час запиту кнопка повинна бути не активною(не
реагувати на клік)." Pagination "Дані про мандрівника повертаються у
відповідному запиті до БД.

Компонент містить в собі: Фото мандрівника. Імʼя мандрівника. Інформацію про
кількість опублікованих статей." TravellerInfo "Компонент містить в собі:
Текст - потрібно взяти з макету відповідно до застосування даного компоненту
(""Цей користувач ще не публікував історій"" або ""У вас ще немає збережених
історій, мершій збережіть вашу першу історію!"" або ""Ви ще нічого не
публікували, поділіться своєю першою історією!""). Посилання стилізоване
підкнопку - ""Назад до історій"" або ""До історій"" або ""Опублікувати
історію"".

Компонент приймає пропси для налаштування контенту та його поведінки: text
(string) - текст з описом. buttonText (string) - текст на кнопці. linkTo
(string) - маршрут на сторінку, на яку повинна відбутись переадресація.

Взаємодія з користувачем: Клік по кнопці потрібно перенаправити користувача на
сторінку ""Історії"" за маршрутом route: /stories, або на сторінку ""Створити
історію"" за маршрутом route: /new-story." MessageNoStories "Компонент
відображає модальне вікно з елементами навігації на сторінки авторизації.

Компонент містить в собі:

- статичний текстовий контент
- кнопку з іконкою ""хрестик"" - для закриття даного модального вікна
- посилання на сторінки логіну та реєстрації - стилізовані під кнопки ""Увійти""
  та ""Зареєструватись"" відповідно.

Взаємодія з користувачем: В результаті кліку на кнопку ""хрестик"", кліку на
бекдроп, натисканню на клавішу ""Escape"" клавіатури модальне вікно повинно
закритись. В результаті кліку на посилання ""Увійти"" модальне вікно повинно
закритись і користувач повинен ути переадресованим на сторінку Логіну. В
результаті кліку на посилання ""Зареєструватись"" модальне вікно повинно
закритись і користувач повинен ути переадресованим на сторінку Реєстрації."
ErrorWhileSavingModal "Універсальний компонент для підвердження або відміни
виконання певної дії, реалізований як модальне вікно, що приймає пропси для
налаштування контенту та поведінки компоненту.

Компоненти: PageTitle - текст ""Ви точно хочете вийти?"". Опис - ""Ми будемо
сумувати за вами!""). Кнопка закриття (""хрестик""). Дві кнопки для
підтвердження та відміни дії, які відправляють запит на бекенд для збереження
інформації в БД.

Пропси: title (string) - текст запитання про підтвердження дії (напр., ""Ви
точно хочете вийти?""). confirmButtonText (string) - текст на кнопці
підтвердження (напр., ""Вийти""). cancelButtonText (string) - текст на кнопці
скасування (напр., ""Відмінити""). onConfirm (function) - колбек-функція, що
виконується при кліку на кнопку підтвердження. onCancel (function) -
колбек-функція, що виконується при кліку на кнопку скасування.

Взаємодія з користувачем: Клік по кнопці підтвердження викликає onConfirm. Клік
по кнопці скасування, по бекдропу, по кнопці закриття ""хрестик"" або натискання
на клавішу Escape повинен закривати модальне вікно." ConfirmModal Layout
"Загальний макет для сторінок. Складається з: -Header - хедер сторінок додатку,
-main - обгортка для основного контенту сторінок, -Footer - футер сторінок
додатку. та рендерить дочірні компоненти (children)." Layout "Компонент
відображається на всіх сторінках та на всіх типах пристроїв. Містить логотип,
основні елементи навігації та блок автентифікації. На сторінках Логіну та
Реєстрації містить в собі лише логотип.

Для НЕавторизованого користувача (Гостя): -Логотип: клік переадресовує на
головну сторінку (/). -Навігація: -посилання ""Головна"" (/). -посилання
""Статті"" (/stories). -посилання ""Еко-Мандрівники"" (/travellers).

AuthBar - панель з елементами навігації на сторінки авторизації.

на планшеті в хедері розміщено логотип, AuthBar та кнопку відкриття бургер-меню.
на мобілці в хедері розміщено логотип та кнопку відкриття бургер-меню.

Для авторизованого користувача: -Логотип: клік переадресовує на головну сторінку
(/). -Навігація: -посилання ""Головна"" (/). -посилання ""Статті"" (/stories).
-посилання ""Еко-Мандрівники"" (/travellers). -посилання ""Мій Профіль""
(/profile). -посилання ""Опублікувати статтю"" (/stories/new)

UserBar - компонент з панеллю користувача.

на планшеті в хедері розміщено логотип, AuthBar та кнопку відкриття бургер-меню.
на мобілці в хедері розміщено логотип та кнопку відкриття бургер-меню.

На планшеті та мобілці всі елементи навігації знаходяться в бургер меню
відповідно до макету." Header "Містить в собі: ""Вхід"" - елемент навігації
стилізований під кнопку, переадресовує на /auth/login. ""Реєстрація"" - елемент
навігації стилізований під кнопку, переадресовує на /auth/register." AuthBar
"Містить в собі: Аватар - за його наявності За відсутності аватару дефолтне
зображення. Ім'я юзера. Кнопка з іконкою, клік по якій відкриває
ConfirmationModal для виходу з системи." UserBar "Компонент містить в собі:
Логотип - аналогічний до логотипу в хедері, клік по якому переаресовує
користувача на головну сторінку, SocialList - список соцмереж, Copyright - текст
""© 2025 Природні Мандри. Усі права захищені."", FooterNav - блок навігації по
сайту." Footer "Компонент містить в собі: Список з переліком посилань на
соц.мережі компанії стилізованих кнопки з іконками відповідних соцмереж:
Facebook - https://www.facebook.com/ Instagram - https://www.instagram.com/  
X - https://x.com/ YouTube - https://www.youtube.com/

Взаємодія з користувачем: В результаті кліку на посилання зі списку, у новій
вкладці повинна відкритись сторінка відповідної соц. мережі" SocialList
"Компонент містить в собі: текст - ""© 2025 Природні Мандри. Усі права
захищені."" значення року - 2025, повинно розраховуватись динамічно і набувати
значення поточного року на момент відкриття сторінки." Copyright "Компонент
відображає елементи навігації по сторінкам додатку.

Компонент містить в собі: Список посилань на сторінки додатку: ""Головна"" -
посилання на Головну сторінку, маршрут (/). ""Статті"" - посилання на Сторінку
зі статтями, маршрут (/stories). ""Еко-Мандрівники"" - посилання на Сторінку з
мандрівниками, маршрут (/travellers)." FooterNav Сторінка "Головна" | HomePage |
route: / "Компонент містить в собі компоненти: Hero, PopularStories, About,
OurTravellers, Join" HomePage "Компонент містить в собі: PageTitle - текст
“Відкрий Україну заново — еко-мандри для натхнення”. Текст з описом секції.
Посилання ""Доєднатись до мандрів"" - якірне посилання до секції ""Join"".
Контентне зораження." Hero "Відображає слайдер за списком популярних історій.
Данні про історії повертаються в результаті відповідного запиту до БД.

Компонент містить в собі: Заголовок секції - текст “Популярні статті”. Посилання
на сторінку ""Історії"" - стилізоване під кнопку ""Всі Статті"". Слайдер зі
списком популярних історій, де кожна історія реалізована за допомогою загального
комопоненту StoryCard. Список складається з 10 елементів. Сладйер відображає: на
десктопі - три статті, на планшеті - дві статті, на мобільній - одна стаття.  
Для реалізації слайдеру потрібно скористатись бібліотекою
(https://swiperjs.com/demos). Слайдер містить кнопки для навігації між картками
слайдера.

Взаємодія з користувачем: Клік по кнопкам вліво/вправо повинен виконувати зсув
слайдів з картками на один у протилежну сторону. У разі досягнення кінця списку
елементів, кнопка відповідно до вказаного напрямку повинна деактевуватись
(задісейблитись).  
Задізейблена кнопка візуальною змінюється, що підкреслює її неактивний стан. "
PopularStories "Компонент містить в собі: Заголовком секції - “Мандруй
екологічно та відкривай нові горизонти”. Текст з описом. Список переваг - кожен
елемент, якого складається з заголовку нижчого рівня ніж у секції та опису.
Контентне зображення." About "Відображає інформацію про зареєтрованих
мандрівників.

Компонент містить в собі: Заголовок секції - текст “Наші Мандрівники”. Посилання
на сторінку мандрівників - стилізоване під кнопку ""Всі мандрівники"". Слайдер
зі списком мандрівників - кожну картку з описом мандрівника реалізовано за
допомогою загального компоненту TravallerCard. Список складається з 12
елементів. Сладйер відображає: на десктопі - 4 картки, на планшеті - 4 картки,
на мобільній - 3 картки.  
Для реалізації слайдеру потрібно скористатись бібліотекою
(https://swiperjs.com/demos). Слайдер містить кнопки для навігації між картками
слайдера.

Взаємодія з користувачем: Клік по кнопкам вліво/вправо повинен виконувати зсув
групи карток що відображаються на один крок у протилежну сторону. (наприклад на
десктопі: 4 картки змінити на 4 інших). У разі досягнення кінця списку
елементів, кнопка відповідно до вказаного напрямку повинна деактевуватись
(задісейблитись).  
Задізейблена кнопка візуальною змінюється, що підкреслює її неактивний стан."
OurTravellers "Компонент містить в собі: Заголовок секції - “Приєднуйся до
спільноти свідомих мандрівників”. Текст з описом. Посилання на сторінки
Реєстрації/Профіля - стилізовані під кнопки ""Зареєструватися""/""Збережені
статті"".

Для авторизованого користувача: клік по посиланню ""Збережені статті""
переадресовує на сторінку (/auth/profile). Для НЕавторизованого користувача:
клік по посиланню ""Зареєструватися"" переадресовує на сторінку реєстрації
(/auth/register)." Join Сторінка "Історії" | StoriesPage | route: /stories
"Компонент відображає сторінку з історіями мандрівників. Дані повертаються в
результаті відповідного запиту до БД.

Компонент містить в собі: PageTitle - текст ""Статті"". CategoriesFilter -
фільтри по категоріям. TravellersStories - загальний компонент зі списком
історій. За замовчуванням компонент відображає картки зі статтями всіх
категорій." StoriesPage "Відображає список кнопок з категоріями для фільтрації
статей по цим категоріям. Дані по наявним категоріям повертаються у відповідному
запиті до БД.

Комопнент містить в собі: Список кнопок з категоріями. За замовчуванням активна
категорія - ""Всі статті""

На мобільній версії: Категорії для фільтрів містяться у випадаючому списку.
Обрана категорія повинна відображатись в полі для вводу випадаючого списку.

Взаємодія з користувачем: В результаті кліку по будь-якій з кнопок або виборі
опції з випадаючого списку для мобілки повинен ініціюватись запит на отримання
першої порції статей відповідної категорії. Обрана категорія повинна отимати
стилі акценту, відповідно до макету. Під час запиту повинен відображатись
індикатор завантаження(лоадер). В результаті помилки на запит юзеру потрібно
вивести пуш-повідомлення з суттю помилки. В результат успішного запиту на
сторінці ""Історій"" повинні з'явитись картки з історіями, дані про які, були
отримані в поточному запиті. Кнопки повинні реагувати на ефекти, змінюючи
курсор, а також змюнюючи стилі відповідно до макету." CategoriesFilter Сторінка
"Історія" | StoryPage | route: /stories/[storyId] "Компонент відображає контент
сторінки з детальним описом історії та списком рекомендованих статей. Дані про
історій та список статей повертаються у відповідному запиті до БД. Якщо дані про
історію відсутні в БД повинен відображатись текст ""Така історія відсутня"". Під
час завантаження сторінки повинен відображатись індикатор завантаження(лоадер).

Компонент містить в собі: StoryDetails - інформація про історію мандрівника.
SaveStory - блок для збереження історії в список збережених історій користувача.
RecomendedStories - список рекомендованих історій." StoryPage "Компонент
відображає інформацію про історію мандрівника.

Компонент містить в собі:

- Зображення (контентне),
- Всі статті + іконка - посилання на сторінку всіх історій мандрівників,
- PageTitle - текст з назвою статті,
- Автор статті - імʼя та прізвище,
- Опубліковано - дата публікації,
- Категорія - назва категорії до якої відноситься дана історія,
- Опис - текст з розповіддю про мандрівку." StoryDetails "Компонент містить в
  собі:
- заголовок - ""Збережіть собі історію"",
- опис - ""Вона буде доступна у вашому профілі у розділі збережене""
- кнопку - ""Зберегти"". якщо дана історія вже знаходиться в списку збережених
  історій корситувача, текст кнопки повинен мати значення ""Видалити зі
  збережених"".

Взаємодія з користувачем:

- НЕ авторизований корстувач: в результаті кліку на кнопку відкривається
  модальне вікно реалізоване загальним компонентом ErrorWhileSavingModal.
- авторизований корстувач: в результаті кліку на кнопку відбувається запит для
  додавання даної історії в список збережених історій користоувача. під час
  запиту повинен відображатись індикатор завантаження(лоадер). в результаті
  помилки запиту потрібно вивести пуш-повідомлення з суттю помилки. в результаті
  успішного запиту текст кнопки повинен змінитись на ""Видалити зі збережених"".
  Кнопка повинна реагувати на ефекти, змінюючи курсор, а також змюнюючи стилі
  відповідно до макету." SaveStory "Компонент містить в собі:
- заголовок - ""Вам також сподобається""
- список StoryCard - картки з рекомендованими історіями, який складається:
  - на десктопній версії з трьох статей,
  - на планшетній - двох,
  - на мобільній - однієї." RecomendedStories Сторінка "Мандрівники" |
    TravallersPage | route: /travellers "Компонент відображає список
    мандрівників(зареєстрованих користувачів). Дані для списку мандрівників
    повретаються у відповідному запиті до БД. Під час завантаження сторінки
    повинен відображатись індикатор завантаження(лоадер).

Компонент містить в собі: PageTitle - текст ""Мандрівники"", TravellersList"
TravallersPage "Компонент відображає список мандрівників(зареєстрованих
користувачів) та пагінацію. Дані для списку мандрівників повретаються у
відповідному запиті до БД.

Компонент містить в собі: Список мандрівників - кожну картку з описом
мандрівника реалізовано за допомогою загального компоненту TravallerCard.
Pagination - загальний компонент, що виконує запити до БД за відповідними
порціями даних.

Взаємодія з користувачем: В результаті помилки на запит користувачу потрібно
вивести пуш-повідомлення з суттю помилки. В результаті успішного запиту, до
списку карток з авторами повинен додатись список карток з даними авторів
отриманими в результаті запиту. Після завантаження останньої порції даних кнопка
пагінації повинна зникнути зі сторінки.

ДОДАТКОВО(не обов'зково): список з картками, створеними на основі отриманих
даних в поточному запиті, повинен автоматично проскролитись до верхньої частини
екрану scrollIntoView" TravellersList Сторінка "Профіль Мандрівника Публічний" |
TravelerPage | route: /travellers/[travellerId] "Дані про мандрівника
повертаються у відповідному запиті до БД. Якщо дані про мандрівника відсутні в
БД повинен відображатись текст ""Такий користувач відсутній"". Під час
завантаження сторінки повинен відображатись індикатор завантаження(лоадер).

Компонент містить в собі: TravellerInfo - загальний компонент, містить
інформацію про мандрівника з travellerId, PageTitle - ""Статті Мандрівника"",
TravellersStories - загальний компонент, містить список статей, які належать
мандрівнику, відображеному на сторінці (з шести елементів на десктопі,
чотирьох - на планшеті та мобільній версії). Якщо у мандрівника відсутні
статті(історії), то замість TravellersStories повинен відображатись загальний
компонент MessageNoStories." TravellerPage Сторінка-профіль "Збережені / Мої
історії" | ProfilePage | route: /profile "ProfilePage доступна лише
авторизованому користувачу.

Компонент містить в собі: TravellerInfo - містить інформацію про даного
авторизованого користувача. ProfileTabs - таби для перемикання ""Збережені
історії"" та ""Мої історії"". TravellersStories містить список статей, які
належать мандрівнику, відображеному на сторінці (з шести елементів на десктопі,
чотирьох - на планшеті та мобільній версії). Якщо у мандрівника відсутні
статті(історії), то замість TravellersStories повинен відображатись загальний
компонент MessageNoStories.

Взаємодія з користувачем: Блоки з історіями користувача відображаються на
відповідних паралельних маршрутах. При зміні маршруту відображається
TravellersStories зі статтями користувача відповідного типу збережені або
власні." ProfilePage "Компонент відображає таби для перемикання між блоками зі
списками збережених історій та власних історій користувача. За замовчуванням
активний таб для відображення блоку збережених історій.

Взаємодія з користувачем: Після вибору іншого перемикача відбувається
переадресація на відповідний паралельний маршрут. При зміні маршруту та
завантаженні даних повинен відображатись індикатор завантаження(лоадер)."
ProfileTabs Сторінка "Створення історії" | AddStory | route: /new-story
"AddStory доступна лише авторизованому користувачу.

Компонент містить в собі: PageTitle - текст ""Створити нову історію"".
AddStoryForm - форма для відправки даних про історію користувача." AddStory
"Компонент отримує дані про історію в поля для вводу від користувача та
відправляє запит на бекенд для збереження інформації в БД.

Містить в собі: Обкладинка статті - зображення статті з доданого в форму файлу.
Завантажити фото - поле для підвантаження файлу з зображенням. Загаловок -
текстове поле для вводу назви історії. Категорія - випадаючий список для вибору
категорії історії. Текст історії - поле для введення опису історії. Кнопки
""Зберегти"" та ""Відмінити"".

За замовучуванням Обкладинка статті відображає зображення плейсхолдера як на
макеті. Кнопка ""Зберегти"" деактивована до поки не будуть заповнені всі поля
форми.

Взаємодія з користувачем: В результаті кліку на кнопку ""Відмінити"" форма
повинна очиститись і набути початкового стану. В результаті кліку на кнопку
""Зберегти"" повинна відбутись перевірка введених даних на валідність
(мінімальна кількість символів та обовʼязкові поля). Валідація полів форми
відбувається за допомогою (Formik, Yup). Якщо введені дані НЕ валідні -
виводяться повідомлення з помилками валідації біля відповідних полів відповідно
до UI Kit макету. Якщо введені дані валідні - відбувається запит на бекенд для
створення історії. Під час запиту повинен відображатись індикатор
завантаження(лоадер). Після успішного запиту потрібно перенаправити користувача
на сторінку з детальним описом новоствореної історії. У разі помилки запиту -
форма залишається відкритою, а користувачу виводиться пуш-повідомлення з суттю
помилки. Кнопки повинні реагувати на ефекти, змінюючи курсор, а також змюнюючи
стилі відповідно до UI Kit макету." AddStoryForm Сторінка "Реєстрація" |
RegisterPage | route: /register "Містить в собі компоненти: AuthHeader
MainAuthNav RegistrationForm AuthFooter" RegisterPage "Компонент містить в собі:

- логотип

Взаємодія з користувачем:

- в результаті кліку по логотипу повинна відбуватись переадресація на головну
  сторінку" AuthHeader " Комопнент містить в собі:
- Реєстрація - посилання для навігації на сторінку реєстрації
- Вхід - посилання для навігації на сторінку логіну

Взаємодія з користувачем: в результаті кліку користувач повинен бути
переадресованим на відповідну сторінку посилання що відподвідає поточній
сторінці повинно мати стилі акценту - підкреслення, як показано на макеті"
MainAuthNav "Надання можливості реєстрації нового користувача.

Взаємодія з користувачем: Користувач заповнює форму з полями: ім’я, email,
пароль. Користувач натискає кнопку ""Зареєструватися"". Після сабміту форми
відбувається перевірка введених даних на валідність. У разі помилок валідації,
вони відображаються згідно з UI Kit. Після успішного запиту користувач
авторизується і перенаправляється на сторінку ""Головна"". У разі помилок в
запитах виводиться пуш-повідомлення з повідомленням відповідної помилки. Кнопка
повинна реагувати на ефекти, змінюючи курсор, а також змюнюючи стилі відповідно
до макету." RegistrationForm "Компонент містить в собі:

- копірайт - текст ""© 2025 Природні Мандри""." AuthFooter Сторінка "Вхід" |
  LoginPage | route: /login "Містить в собі компоненти: AuthHeader MainAuthNav
  LoginForm AuthFooter" AuthPage "Надання можливості входу існуючого
  користувача.

Взаємодія з користувачем: Користувач вводить email та пароль. Користувач
натискає кнопку ""Увійти"". Після натискання на кнопку відбувається валідація
полів форми за допомогою (Formik, Yup). У разі помилок валідації, вони
відображаються згідно з UI Kit. Після успішного запиту користувач авторизується
і перенаправляється на сторінку ""Головна"". У разі помилок в запитах виводиться
пуш-повідомлення з повідомленням відповідної помилки. Кнопка повинна реагувати
на ефекти, змінюючи курсор, а також змюнюючи стилі відповідно до макету."
LoginForm ДОДАТКОВІ ЗАВДАННЯ (ВИКОНУЮТЬСЯ ЗА УМОВИ, ЩО БУЛО РЕАЛІЗОВАНО ВСІ
ОСНОВНІ) Сторінка "Персональна інформація (редагування профілю)" | PersonalPage
| route: /profile/edit "Додаткове завдання: Реалізувати логіку редагування
профілю мандрівника. Реалізація на вибір команди в концепції стилів додатку."
PersonalPage Тема додатку Реалізувати темну тему додатку.
